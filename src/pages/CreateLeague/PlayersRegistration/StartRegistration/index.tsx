import { Container, Typography } from '@mui/material';

const StartRegistration = () => {
  return (
    <Container>
      <Typography>Start Registration</Typography>
    </Container>
  );
};

export default StartRegistration;
