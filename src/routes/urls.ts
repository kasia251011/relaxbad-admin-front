module PATH_CREATE_LEAGUE {
  export const CREATE_LEAGUE = '/';
  export const START = '/start';
  export const STEP = '/step';
  export const CREATE_GROUPS = `${STEP}/create-groups`;
  export const START_REGISTRATION = `${STEP}/start-registration`;
  export const SUBMIT_PLAYERS = `${STEP}/submit-players`;
}

export default PATH_CREATE_LEAGUE;
